msg.str.ascii <- function(x, msg = NULL, key = NULL, ...){
   # MSG.STR.ASCII - Print a message for an 'ascii' object.

   # Get index key if none is specified:
   if (is.null(key)) key <- key(x)
   
   msg <- msg.str.data.frame(x, msg = msg, key = key, ...)
   
   return(msg)
}
